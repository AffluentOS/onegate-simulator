# Shared password to enter the simulator (set a strong value in Coolify)
APP_PASSWORD=change-me

# Long random string used as the session cookie value (set in Coolify)
# Generate: openssl rand -hex 32
SESSION_TOKEN=change-me-to-a-long-random-string
