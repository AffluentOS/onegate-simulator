# Postgres connection (set in Coolify to the onegate-db internal URL)
DATABASE_URL=postgres://user:pass@host:5432/onegate

# Secret used to sign session tokens. Generate: openssl rand -hex 32
AUTH_SECRET=change-me-to-a-long-random-string
